# 第16章 前后端集成实战——完整数据流与状态管理

## 本章目标

1. 掌握Fetch API封装与统一错误处理机制
2. 深入理解SSE流式数据的前端解析(TextDecoderStream + TransformStream)
3. 学会使用Pinia管理全局状态(用户、业务、UI状态)
4. 理解JWT Token的存储、刷新与自动续期策略
5. 掌握文件上传组件实现与进度显示

---

## 一、API封装与错误处理

### 1. 统一API封装

**api/request.ts** (基础封装):

```typescript
import { useUserStore } from '@/store/user'

// 基础请求函数
export async function request<T = any>(
  url: string,
  options: RequestInit = {}
): Promise<T> {
  const userStore = useUserStore()
  const token = userStore.getUserToken()

  // 合并默认配置
  const defaultOptions: RequestInit = {
    mode: 'cors',
    headers: {
      'Content-Type': 'application/json',
      ...(token && { 'Authorization': `Bearer ${token}` }),
    },
  }

  const finalOptions = { ...defaultOptions, ...options }

  try {
    const response = await fetch(url, finalOptions)

    // 401未授权 → 跳转登录
    if (response.status === 401) {
      userStore.clearToken()
      window.location.href = '/login'
      throw new Error('未授权,请先登录')
    }

    // 非200状态码
    if (!response.ok) {
      const error = await response.json()
      throw new Error(error.message || '请求失败')
    }

    return await response.json()
  } catch (error) {
    console.error('API错误:', error)
    throw error
  }
}

// GET请求
export const get = <T = any>(url: string, params?: Record<string, any>): Promise<T> => {
  if (params) {
    const query = new URLSearchParams(params).toString()
    url = `${url}?${query}`
  }
  return request<T>(url, { method: 'GET' })
}

// POST请求
export const post = <T = any>(url: string, data?: any): Promise<T> => {
  return request<T>(url, {
    method: 'POST',
    body: JSON.stringify(data),
  })
}
```

### 2. 业务API封装

**api/index.ts**(之前读取的代码):

```typescript
import { useUserStore } from '@/store/user'

/**
 * 用户登录
 */
export async function login(username: string, password: string) {
  const url = new URL(`${location.origin}/sanic/user/login`)
  const req = new Request(url, {
    mode: 'cors',
    method: 'post',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      username,
      password,
    }),
  })
  return fetch(req)
}

/**
 * Event Stream 调用大模型接口(SSE)
 */
export async function createOllama3Stylized(
  text: string,
  qa_type: string,
  uuid: string,
  chat_id: string,
  file_list: any[]
) {
  const userStore = useUserStore()
  const token = userStore.getUserToken()
  const url = new URL(`${location.origin}/sanic/dify/get_answer`)

  // 创建 AbortController 用于超时控制
  const controller = new AbortController()
  const timeoutId = setTimeout(() => {
    controller.abort()
  }, 10 * 60 * 1000) // 10分钟超时

  const req = new Request(url, {
    mode: 'cors',
    method: 'post',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`,
    },
    body: JSON.stringify({
      query: text,
      qa_type,
      uuid,
      chat_id,
      file_list,
    }),
    signal: controller.signal, // 添加超时信号
  })

  return fetch(req).finally(() => {
    clearTimeout(timeoutId) // 清除超时定时器
  })
}

/**
 * 停止对话
 */
export async function stop_chat(task_id: string, qa_type: string) {
  const userStore = useUserStore()
  const token = userStore.getUserToken()
  const url = new URL(`${location.origin}/sanic/dify/stop_chat`)

  const req = new Request(url, {
    mode: 'cors',
    method: 'post',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`,
    },
    body: JSON.stringify({
      task_id,
      qa_type,
    }),
  })
  return fetch(req)
}
```

**API设计要点**:

1. **统一URL构建**: 使用`new URL()`避免路径拼接错误
2. **AbortController**: 支持请求取消和超时
3. **Token自动注入**: 从store读取,无需手动传递

---

## 二、SSE流式数据处理(重点)

### 1. 完整数据流处理

**store/business/index.ts**(之前读取的代码,完整分析):

```typescript
import { defineStore } from 'pinia'
import * as GlobalAPI from '@/api'
import * as TransformUtils from '@/components/MarkdownPreview/transform'

export const useBusinessStore = defineStore('business-store', {
  state: (): BusinessState => {
    return {
      writerList: {},       // t04图表数据
      qa_type: 'COMMON_QA', // 问答类型
      file_list: [],        // 文件列表
      task_id: '',          // 任务ID
    }
  },
  actions: {
    /**
     * Event Stream 调用大模型python服务接口
     */
    async createAssistantWriterStylized(
      uuid: string,
      chat_id: string,
      writerOid: string,
      data: any
    ): Promise<{
        error: number
        reader: ReadableStreamDefaultReader<string> | null
        needLogin: boolean
      }> {
      return new Promise((resolve) => {
        const query_str = data.text
        const file_list = data.file_list

        const processResponse = (res) => {
          // 处理401未授权
          if (res.status === 401) {
            return {
              error: 1,
              reader: null,
              needLogin: true,
            }
          }

          // 处理200成功
          else if (res.status === 200) {
            // ========= 核心流式数据处理 =========
            const reader = res.body
              // Step 1: 解码字节流为UTF-8字符串
              .pipeThrough(new TextDecoderStream())

              // Step 2: 按换行符分割消息
              .pipeThrough(TransformUtils.splitStream('\n'))

              // Step 3: 解析JSON并路由dataType
              .pipeThrough(
                new TransformStream({
                  transform: (chunk, controller) => {
                    try {
                      const jsonChunk = JSON.parse(
                        chunk.split('data:')[1],
                      )

                      // 保存task_id
                      if (jsonChunk.task_id) {
                        this.update_task_id(jsonChunk.task_id)
                      }

                      // 路由不同类型数据
                      switch (jsonChunk.dataType) {
                        case 't11':  // 任务ID
                          controller.enqueue(
                            JSON.stringify({
                              content: `问题: ${query_str}`,
                            }),
                          )
                          break

                        case 't02':  // 文本内容
                          if (
                            jsonChunk.data
                            && jsonChunk.data.content
                          ) {
                            controller.enqueue(
                              JSON.stringify(
                                jsonChunk.data,
                              ),
                            )
                          }
                          break

                        case 't04':  // 图表数据
                          this.writerList = jsonChunk
                          break

                        default:
                          // 其他类型
                      }
                    } catch (e) {
                      console.error(
                        'Error processing chunk:',
                        e,
                      )
                    }
                  },
                  flush: (controller) => {
                    controller.terminate()
                  },
                }),
              )
              .getReader()

            return {
              error: 0,
              reader,
              needLogin: false,
            }
          }

          // 处理其他错误
          else {
            return {
              error: 1,
              reader: null,
              needLogin: false,
            }
          }
        }

        // 调用后端接口
        GlobalAPI.createOllama3Stylized(query_str, this.qa_type, uuid, chat_id, file_list)
          .then((res) => resolve(processResponse(res)))
          .catch((err) => {
            console.error('Request failed:', err)
            resolve({
              error: 1,
              reader: null,
              needLogin: false,
            })
          })
      })
    },
  },
})
```

**数据流管道详解**:

```
ReadableStream(字节流)
    ↓ pipeThrough(TextDecoderStream)
UTF-8字符串流
    ↓ pipeThrough(splitStream('\n'))
按换行符分割的消息流
    ↓ pipeThrough(TransformStream)
解析JSON + 路由dataType
    ↓ getReader()
ReadableStreamDefaultReader
```

### 2. splitStream工具函数

**components/MarkdownPreview/transform.ts**(推测实现):

```typescript
export function splitStream(delimiter: string) {
  let buffer = ''

  return new TransformStream({
    transform(chunk: string, controller) {
      buffer += chunk
      const lines = buffer.split(delimiter)

      // 保留最后一个不完整的行
      buffer = lines.pop() || ''

      // 输出完整的行
      lines.forEach((line) => {
        if (line.trim()) {
          controller.enqueue(line)
        }
      })
    },
    flush(controller) {
      // 处理剩余数据
      if (buffer.trim()) {
        controller.enqueue(buffer)
      }
    },
  })
}
```

**工作原理**:

```typescript
// 输入流
"data:{...}\n\ndata:{...}\n\n"

// 经过splitStream('\n')后
["data:{...}", "", "data:{...}", ""]

// 过滤空字符串
["data:{...}", "data:{...}"]
```

### 3. 组件中使用SSE

**views/ChatPage.vue**:

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { useBusinessStore } from '@/store/business'

const businessStore = useBusinessStore()
const messages = ref<any[]>([])
const isLoading = ref(false)

const sendMessage = async (text: string) => {
  // 添加用户消息
  messages.value.push({
    id: Date.now(),
    sender: 'user',
    content: text,
  })

  isLoading.value = true

  // 创建AI消息占位
  const aiMessage = {
    id: Date.now() + 1,
    sender: 'ai',
    content: '',
    streaming: true,
  }
  messages.value.push(aiMessage)

  try {
    // 调用SSE接口
    const result = await businessStore.createAssistantWriterStylized(
      'uuid-001',
      'chat-001',
      'writer-001',
      { text, file_list: [] }
    )

    if (result.error) {
      console.error('SSE调用失败')
      return
    }

    const reader = result.reader!

    // 读取流式数据
    while (true) {
      const { done, value } = await reader.read()
      if (done) break

      // 解析消息
      const data = JSON.parse(value)

      // 追加内容
      if (data.messageType === 'continue') {
        aiMessage.content += data.content
      }
    }

    // 结束流式输出
    aiMessage.streaming = false
  } catch (error) {
    console.error('SSE错误:', error)
  } finally {
    isLoading.value = false
  }
}
</script>

<template>
  <div class="chat-page">
    <div class="message-list">
      <ChatMessage
        v-for="msg in messages"
        :key="msg.id"
        :content="msg.content"
        :sender="msg.sender"
        :streaming="msg.streaming"
      />
    </div>

    <InputArea
      :disabled="isLoading"
      @send="sendMessage"
    />
  </div>
</template>
```

---

## 三、Pinia状态管理实战

### 1. User Store(用户状态)

**store/user.ts**:

```typescript
import { defineStore } from 'pinia'

export const useUserStore = defineStore('user', {
  state: () => ({
    token: localStorage.getItem('token') || '',
    userInfo: null as any,
  }),

  getters: {
    // 获取Token
    getUserToken: (state) => () => state.token,

    // 是否登录
    isLogin: (state) => !!state.token,
  },

  actions: {
    // 设置Token
    setToken(token: string) {
      this.token = token
      localStorage.setItem('token', token)
    },

    // 清除Token
    clearToken() {
      this.token = ''
      this.userInfo = null
      localStorage.removeItem('token')
    },

    // 登录
    async login(username: string, password: string) {
      const response = await fetch('/sanic/user/login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ username, password }),
      })

      const data = await response.json()

      if (data.code === 200) {
        this.setToken(data.data.token)
        this.userInfo = data.data.userInfo
        return true
      }

      return false
    },

    // 登出
    logout() {
      this.clearToken()
      window.location.href = '/login'
    },
  },
})
```

### 2. Business Store(业务状态)

**store/business/index.ts**(之前已详细分析):

```typescript
export const useBusinessStore = defineStore('business-store', {
  state: () => ({
    writerList: {},       // t04图表数据
    qa_type: 'COMMON_QA', // 问答类型
    file_list: [],        // 上传的文件列表
    task_id: '',          // 当前任务ID
  }),

  actions: {
    // 更新问答类型
    update_qa_type(qa_type: string) {
      this.qa_type = qa_type
    },

    // 添加文件
    add_file(file_url: any) {
      this.file_list.push(file_url)
    },

    // 清空文件列表
    clear_file_list() {
      this.file_list = []
    },

    // 删除单个文件
    remove_file(source_file_key: string) {
      const index = this.file_list.findIndex(
        (file) => file.source_file_key === source_file_key,
      )
      if (index !== -1) {
        this.file_list.splice(index, 1)
      }
    },
  },
})
```

### 3. UI Store(界面状态)

**store/ui.ts**:

```typescript
export const useUIStore = defineStore('ui', {
  state: () => ({
    sidebarCollapsed: false,  // 侧边栏折叠状态
    darkMode: false,          // 暗黑模式
    showSettings: false,      // 设置面板
  }),

  actions: {
    toggleSidebar() {
      this.sidebarCollapsed = !this.sidebarCollapsed
    },

    toggleDarkMode() {
      this.darkMode = !this.darkMode
      document.documentElement.classList.toggle('dark', this.darkMode)
    },
  },

  // 持久化
  persist: {
    enabled: true,
    strategies: [
      {
        key: 'ui-settings',
        storage: localStorage,
      },
    ],
  },
})
```

---

## 四、文件上传组件

### 1. 基础上传组件

**components/FileUpload.vue**:

```vue
<template>
  <div class="file-upload">
    <input
      ref="fileInputRef"
      type="file"
      :accept="props.accept"
      multiple
      style="display: none"
      @change="handleFileChange"
    />

    <button @click="selectFile">
      📎 选择文件
    </button>

    <!-- 文件列表 -->
    <div v-if="uploadedFiles.length" class="file-list">
      <div
        v-for="file in uploadedFiles"
        :key="file.source_file_key"
        class="file-item"
      >
        <span>{{ file.file_name }}</span>
        <span>{{ file.file_size }}</span>
        <button @click="removeFile(file.source_file_key)">删除</button>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useBusinessStore } from '@/store/business'

interface Props {
  accept?: string  // 接受的文件类型
}

const props = withDefaults(defineProps<Props>(), {
  accept: '.xlsx,.xls,.csv'
})

const businessStore = useBusinessStore()
const fileInputRef = ref<HTMLInputElement>()
const uploadedFiles = ref<any[]>([])

// 选择文件
const selectFile = () => {
  fileInputRef.value?.click()
}

// 处理文件变化
const handleFileChange = async (e: Event) => {
  const target = e.target as HTMLInputElement
  const files = target.files
  if (!files) return

  for (const file of files) {
    await uploadFile(file)
  }

  // 重置input,允许重复上传同一文件
  target.value = ''
}

// 上传文件
const uploadFile = async (file: File) => {
  const formData = new FormData()
  formData.append('file', file)

  try {
    const response = await fetch('/sanic/file/upload_file_and_parse', {
      method: 'POST',
      body: formData,
    })

    const result = await response.json()

    if (result.code === 200) {
      const fileData = {
        source_file_key: result.data.source_file_key,
        parse_file_key: result.data.parse_file_key,
        file_name: file.name,
        file_size: result.data.file_size,
      }

      uploadedFiles.value.push(fileData)
      businessStore.add_file(fileData)
    }
  } catch (error) {
    console.error('上传失败:', error)
  }
}

// 删除文件
const removeFile = (source_file_key: string) => {
  uploadedFiles.value = uploadedFiles.value.filter(
    f => f.source_file_key !== source_file_key
  )
  businessStore.remove_file(source_file_key)
}
</script>

<style scoped lang="scss">
.file-upload {
  padding: 16px;

  .file-list {
    margin-top: 12px;

    .file-item {
      display: flex;
      justify-content: space-between;
      padding: 8px;
      background: #f5f5f5;
      border-radius: 4px;
      margin-bottom: 8px;
    }
  }
}
</style>
```

### 2. 拖拽上传

**增强版FileUpload**:

```vue
<template>
  <div
    class="file-upload-area"
    :class="{ dragging: isDragging }"
    @drop.prevent="handleDrop"
    @dragover.prevent="isDragging = true"
    @dragleave="isDragging = false"
  >
    <p>拖拽文件到这里 或 点击上传</p>
    <input type="file" @change="handleFileChange" />
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

const isDragging = ref(false)

const handleDrop = (e: DragEvent) => {
  isDragging.value = false
  const files = e.dataTransfer?.files
  if (files) {
    handleFiles(files)
  }
}

const handleFiles = async (files: FileList) => {
  for (const file of files) {
    await uploadFile(file)
  }
}
</script>

<style scoped lang="scss">
.file-upload-area {
  border: 2px dashed #ccc;
  padding: 40px;
  text-align: center;
  transition: all 0.3s;

  &.dragging {
    border-color: #667eea;
    background: rgba(102, 126, 234, 0.1);
  }
}
</style>
```

---

## 五、JWT Token管理

### 1. Token存储

**方案对比**:

| 存储方式 | 优点 | 缺点 | 适用场景 |
|---------|------|------|---------|
| **localStorage** | 简单,持久化 | XSS风险 | 普通应用 |
| **sessionStorage** | 关闭页面清除 | XSS风险,不能跨标签页 | 临时会话 |
| **Cookie(HttpOnly)** | 防XSS | CSRF风险 | 安全要求高 |
| **内存** | 最安全 | 刷新页面丢失 | 高安全应用 |

**本项目方案**: localStorage(便于开发)

```typescript
// store/user.ts
setToken(token: string) {
  this.token = token
  localStorage.setItem('token', token)
}
```

### 2. Token刷新

**需求**: Token有效期24小时,快过期时自动刷新

```typescript
// utils/token.ts
import { jwtDecode } from 'jwt-decode'

// 检查Token是否即将过期(1小时内)
export function isTokenExpiringSoon(token: string): boolean {
  try {
    const decoded: any = jwtDecode(token)
    const exp = decoded.exp * 1000  // 转为毫秒
    const now = Date.now()
    return exp - now < 60 * 60 * 1000  // 1小时
  } catch {
    return true
  }
}

// 刷新Token
export async function refreshToken(oldToken: string): Promise<string> {
  const response = await fetch('/sanic/user/refresh_token', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${oldToken}`,
    },
  })

  const data = await response.json()
  return data.data.token
}
```

**自动刷新逻辑**:

```typescript
// main.ts
import { useUserStore } from '@/store/user'
import { isTokenExpiringSoon, refreshToken } from '@/utils/token'

const userStore = useUserStore()

// 每5分钟检查一次
setInterval(async () => {
  const token = userStore.getUserToken()

  if (token && isTokenExpiringSoon(token)) {
    try {
      const newToken = await refreshToken(token)
      userStore.setToken(newToken)
    } catch {
      userStore.logout()
    }
  }
}, 5 * 60 * 1000)
```

---

## 六、完整数据流示例

### 1. 用户登录流程

```
用户输入账号密码
    ↓
调用login API
    ↓
后端验证成功,返回Token
    ↓
存储到userStore
    ↓
localStorage持久化
    ↓
跳转到聊天页面
```

### 2. 发送消息流程

```
用户输入消息点击发送
    ↓
添加用户消息到messages数组
    ↓
创建AI消息占位(content='')
    ↓
调用createOllama3Stylized(SSE)
    ↓
.pipeThrough(TextDecoderStream)
    ↓
.pipeThrough(splitStream)
    ↓
.pipeThrough(TransformStream)
    ↓
根据dataType路由:
  ├─ t02 → 追加到AI消息content
  ├─ t04 → 保存到writerList(图表)
  └─ t08 → 标记流结束
    ↓
渲染完成,停止loading
```

### 3. 文件上传流程

```
用户选择Excel文件
    ↓
FormData包装文件
    ↓
POST /sanic/file/upload_file_and_parse
    ↓
后端解析文件,返回file_key
    ↓
保存到businessStore.file_list
    ↓
发送消息时自动携带file_list
    ↓
后端Excel Agent处理
```

---

## 七、本章小结

### 1. 核心技术栈

```
Fetch API            → HTTP请求
SSE + Streams API    → 流式数据处理
Pinia                → 状态管理
localStorage         → 数据持久化
FormData             → 文件上传
```

### 2. 关键代码清单

| 功能 | 代码位置 | 关键技术 |
|------|---------|---------|
| API封装 | api/request.ts | Fetch + 错误处理 |
| SSE处理 | store/business/index.ts | pipeThrough流水线 |
| 用户状态 | store/user.ts | Pinia + localStorage |
| 文件上传 | components/FileUpload.vue | FormData |
| Token刷新 | utils/token.ts | jwt-decode |

### 3. 数据流架构

```
组件(UI) ↔ Pinia(状态) ↔ API(请求) ↔ 后端
            ↕
      localStorage(持久化)
```

### 4. 教程总结

**全教程回顾**(16章完整内容):

**第一部分: 基础架构篇**(1-4章)

- Sanic异步框架 + UV包管理
- SQLAlchemy 2.0双引擎 + JWT认证

**第二部分: 智能体基础篇**(5-8章)

- LangChain/LangGraph核心概念
- CommonReact Agent + Text2SQL Agent

**第三部分: 智能体应用篇**(9-12章)

- Excel Agent实战
- SSE流式响应 + Neo4j/MinIO
- Docker容器化部署

**第四部分: 前端开发篇**(13-16章)

- Vue3 + Vite框架搭建
- 核心UI组件实现
- 前后端集成 + 状态管理

**技术栈总览**:

```
前端: Vue 3 + TypeScript + Vite + Pinia + Naive UI
后端: Python 3.11 + Sanic + SQLAlchemy + LangChain
智能体: LangGraph + MCP + DashScope API
数据存储: MySQL + Neo4j + MinIO + FAISS
部署: Docker + Docker Compose
```

**恭喜你完成了全部16章教程!**

你现在已具备:

1. ✓ 从零搭建全栈AI问答系统
2. ✓ 深入理解Text2SQL工作流程
3. ✓ 掌握SSE流式响应实现
4. ✓ 学会Docker容器化部署
5. ✓ 理解前后端分离架构

**下一步建议**:

1. **动手实践**: 部署项目到云服务器
2. **功能扩展**: 添加语音输入、多模型切换
3. **性能优化**: Redis缓存、数据库读写分离
4. **开源贡献**: 向社区分享你的改进

**持续学习,不断精进!** 🚀

---

**思考题**:

1. 为什么SSE比WebSocket更适合AI流式输出场景?
2. 如何实现Token无感刷新(用户无需重新登录)?
3. 如何优化大文件上传的性能(断点续传、分片上传)?
